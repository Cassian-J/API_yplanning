info:
  name: GetUser
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/user/"
  body:
    type: json
    data: |-
      {
        "username":"",
        "email":""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
