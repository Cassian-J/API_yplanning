info:
  name: DeleteUser
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/user/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
