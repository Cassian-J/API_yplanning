info:
  name: CreateColor
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/color/"
  body:
    type: json
    data: |-
      {
        "hex_code":"",
        "name":""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
