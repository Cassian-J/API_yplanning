info:
  name: GetColorByID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/color/:ColorID"
  params:
    - name: ColorID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
