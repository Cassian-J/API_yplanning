info:
  name: DeleteColor
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/color/:ColorID"
  params:
    - name: ColorID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
