info:
  name: GetByHexCode
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/color/hex"
  body:
    type: json
    data: |-
      {
        "hex_code":"",
        "name":""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
