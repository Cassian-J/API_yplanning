info:
  name: DeleteGroup
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/group/:GroupID"
  params:
    - name: GroupID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
