info:
  name: CreateGroupe
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/group/"
  body:
    type: json
    data: |-
      {
        "name":"",
        "creator_id":""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
