info:
  name: GetGroupByID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/group/:GroupID"
  params:
    - name: GroupID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
