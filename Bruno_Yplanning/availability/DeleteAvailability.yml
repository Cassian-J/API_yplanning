info:
  name: DeleteAvailability
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/availability/:AvailabilityID"
  params:
    - name: AvailabilityID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
