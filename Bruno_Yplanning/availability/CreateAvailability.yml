info:
  name: CreateAvailability
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/availability/"
  body:
    type: json
    data: |-
      {
        "date_begin":"",
        "date_end":"",
        "user_id":""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
