info:
  name: GetAvailabilityByID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/availability/:AvailabilityID"
  params:
    - name: AvailabilityID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
