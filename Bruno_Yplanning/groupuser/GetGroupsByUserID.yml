info:
  name: GetGroupsByUserID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/group-user/user/:UserID"
  params:
    - name: UserID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
