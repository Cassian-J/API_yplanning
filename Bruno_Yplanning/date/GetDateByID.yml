info:
  name: GetDateByID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/date/:DateID"
  params:
    - name: DateID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
