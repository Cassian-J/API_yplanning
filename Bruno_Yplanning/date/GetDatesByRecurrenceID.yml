info:
  name: GetDatesByRecurrenceID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/date/recurrence/:RecurrenceID"
  params:
    - name: RecurrenceID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
