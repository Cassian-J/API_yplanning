info:
  name: GetDatesByUserID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/date/user/:UserID"
  params:
    - name: UserID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
