info:
  name: DeleteDate
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/date/:DateID"
  params:
    - name: DateID
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
